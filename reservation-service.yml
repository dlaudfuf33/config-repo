server:
  port: 8081

spring:
  application:
    name: reservation-service

  datasource:
    url: jdbc:mysql://localhost:3306/petclinic_reservation
    username: root
    password: dkrakek1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  show-sql: true
  format-sql: true
